#include<conio.h>
#include<stdio.h>

void main()
{
   char *filename;
   FILE *fp1,*fp2;
   int i,number;

clrscr();

   fp1=fopen("TEST.txt","w");
	for(i=10;i<=100;i+=10)
		putw(i,fp1);
	fclose(fp1);

	printf("\n Input The  File Name: ");

	open_file:
	scanf("%s",filename);
	 fp2=fopen(filename,"r");
//	if((fp2=fopen(filename,"r")) == NULL)
	if (fp2==NULL)
	{
	    printf("\nCan not open File:");
	    printf("\nType File name again:");
	    goto open_file;
	}

	else
		for(i=1;i<=20;i++)
//		while(feof(fp2)==0)
		{
		  number=getw(fp2);
		  if (feof(fp2)!=0)
			{
				printf("\n Ran Out Of Data\n");
				break;
			}
		 /* if(feof(fp2))
		  {
			printf("\n Ran Out Of Data:\n");
			break;
		  }
		  else*/
			printf("%d\n",number);
		  }

		  fclose(fp2);
		  getch();
}
